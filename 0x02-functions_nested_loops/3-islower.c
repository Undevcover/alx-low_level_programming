#include "main.h"
/**
 * my_function - This is a description
 */

int _islower(int c) 
{
	int letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
		if (letter)
		{
			_putchar(0);
		}
	}	
	_putchar(1);
}
